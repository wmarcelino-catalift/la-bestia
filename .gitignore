# Personal / local
CLAUDE.local.md
_archive/

# OS
.DS_Store
._*
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Logs and runtime memory (generated by hooks at runtime, never committed)
logs/
agent-memory/
*.jsonl

# Backups created by install.sh
**/_backup_*

# Eval reports (generated)
evals/_reports/

# Node (transient, used only by ajv-cli local installs)
node_modules/
package-lock.json

# Live Claude Code state (project-local installs put it under .claude/)
**/.claude/logs/
**/.claude/file-history/
**/.claude/projects/
**/.claude/sessions/
**/.claude/shell-snapshots/
**/.claude/cache/

# Secrets — defense in depth (block-secrets.sh + permissions deny + here)
*.env
*.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*credentials*.json
*service-account*.json
*firebase-adminsdk*.json
id_rsa*
.npmrc
.pypirc
