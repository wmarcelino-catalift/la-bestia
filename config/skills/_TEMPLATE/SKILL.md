---
name: skill-name
version: 0.1.0
description: "<one-sentence outcome the skill produces>. Auto-trigger on '<kw1>', '<kw2>'."
triggers: [keyword1, keyword2]
---

# Skill: <name>

> Short metadata above this line is loaded eagerly (~100 tokens).
> Body below is loaded only when triggered (progressive disclosure).

## When to use

Concrete trigger conditions. The skill activates when the user prompt matches a `triggers:` keyword OR a configured threshold (e.g., context > 60%).

## Recipe

Numbered steps, copy-pasteable.

1. ...
2. ...
3. ...

## Anti-patterns

- Don't use this skill when `<X>` — use `<other-skill>` instead.
- Don't use this skill if `<Y>` — escalate to operator.

## Versioning

| Version | Date       | Change  |
| ------- | ---------- | ------- |
| 0.1.0   | YYYY-MM-DD | initial |
