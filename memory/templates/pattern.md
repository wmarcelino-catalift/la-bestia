# Pattern — <short imperative title>

> Reusable recipe discovered in this repo. Belongs in `memory/patterns/<slug>.md`. Replace this header.

## When to use

Concrete trigger conditions. A reader should be able to tell in 10 seconds whether this pattern applies to their problem.

- Trigger: ...
- Context: ...
- Stack: ...

## When NOT to use

Equally important. Patterns mis-applied are worse than no patterns.

- Anti-trigger: ...
- Smell: if you see X, this pattern is wrong; consider Y instead.

## Recipe

Numbered steps. Concrete. Copy-pasteable where possible.

1. ...
2. ...
3. ...

## Example (real, from this repo)

A small but complete example, ideally from a real commit or file in this repo. Include the file path so a future reader can grep.

```
// path/to/example.ts
...
```

## Why it works

One paragraph on the underlying principle. Keep it short — this is the "why" so the reader knows when to deviate.

## Common mistakes

1. Mistake → consequence → fix.
2. ...

## Related

- ADR: `memory/decisions/<NNNN>-<slug>.md` (if a decision triggered the pattern)
- Other patterns: `memory/patterns/<other-slug>.md`
